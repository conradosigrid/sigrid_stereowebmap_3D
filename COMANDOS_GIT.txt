*******************************************************
REPOSITORIO NUEVO
*******************************************************
Siempre sigue este orden cuando creas un repo nuevo:
1 Crear repo en GitHub vacío
2 git init local
3 git add .
4 git commit
5 git remote add origin
6 git push -u origin main


*******************************************************
CLONAR REPOSITORIO EN OTRO ORDENADOR
*******************************************************
Clonar el proyecto en otro ordenador:
git clone https://github.com/tu_usuario/tu_repositorio.git
Desde la terminal de VS Code
cd %APPDATA%\QGIS\QGIS4\profiles\QGIS_4_DEV\python\plugins
git checkout main
git pull


*******************************************************
FLUJO DE TRABAJO
*******************************************************
Al empezar una tarea:

1) Descargar la versión actual del repositorio en nuestro local:
   - Conectar a la rama main
   - Hacer Pull
2) Crea nuestra rama de trabajo
   - Create branch.
3) Ya trabajar sobre esa rama 
   - se puede hacer STAGE para cambios pequeños temporales aunque no sea estable
   - Hacer COMMIT para cambios más definitivos y estables, donde se puede meter explicación
4) Juntar la rama con el main
   - Cada pocos commits, a ser posible cada día, fusionamos código
   - Push de la rama, ya la sube al repositorio
   - COMPARE & PULL REQUEST. Esto se hace desde la web, botón verde. (no lo he visto desde el local)
         * Te dice si hay conflictos en el MERGE y te pide arreglarlos
   - Si todo va bien, MERGE PULL REQUEST ya lo junta con main
      * adicionalmente se puede borrar la rama en ese momento. 

Y a partir de ese punto, volver a empezar desde 1) IMPORTANTE el PULL para incorporar los cambios del otro.




En forma de comandos para manejar git desde la terminal de VS Code (ctrl+ñ):
Al empezar una tarea:
git checkout main
git pull        # antes de empezar
git checkout -b NOMBRE_RAMA (que puede ser fix/visualizacion o  feature/capas de puntos o doc/documento1 ...)
es lo mismo que:
git switch -c NOMBRE_RAMA

TRABAJAMOS EN LA RAMA

HACEMOS COMMITS PEQUEÑOS Y CLAROS

git add .    o git add archivo.py
git commit -m "Descripción del cambio"

CUANDO YA ESTÉ BIEN, SUBIR LA RAMA A GITHUB

git push -u origin NOMBRE_RAMA

FINALMENTE; EL MOMENTO DE JUNTAR LOS CAMBIOS AL MAIN:
 Botón “Compare & Pull Request” (PR) para ver y resolver conflictos
 Los solucionamos en la misma rama y el PR se va actualizando solo
 
CUANDO EL PR ESTÉ BIEN, MERGE A MAIN
Merge Pull Request
Se puede borrar la rama al acabar opcionalmente:
git branch -d feature/modelo-3d

Y no olvidar sincronizarnos todos (antes de empezar cualquier cosa):
git checkout main
git pull

OTROS COMANDOS:
Para visualizar las ramas y commits:
git branch
git log --oneline --graph --all
